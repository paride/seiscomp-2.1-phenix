C--DIMENSION THE TRAVEL TIME, WEIGHT, RESIDUAL & IMPORTANCE ARRAYS
C--FOR HYPOINVERSE. USE VIRTUAL MEMORY ON THE PC350.

C--VAX VERSION ONLY:
	DIMENSION A(MMAX,5)
C!TRAVEL TIMES & DERIVATIVES
	DIMENSION R (MMAX)	
C!CURRENT TRAVEL TIME RESIDUAL
	DIMENSION W (MMAX)	
C!COMPUTED WEIGHT OF READING
	DIMENSION IMPORT (MMAX)	
C!COMPUTED IMPORTANCE OF READING IN .001
	DIMENSION MTCAL (MMAX)
C!CALC TT IN .01. MULT S TIMES BY POS

C--PC350 VERSION ONLY:
C	VIRTUAL A(MMAX,5),R(MMAX),W(MMAX),IMPORT(MMAX),MTCAL(MMAX)
